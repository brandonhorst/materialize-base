filters:
  and:
    - 'file.inFolder("projects")'
views:
  - type: table
    name: Invalid Regex View
    filters:
      and:
        - 'RegExp("(").matches(note.title || "")'
    order:
      - note.title
